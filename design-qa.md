# Agent 首页设计 QA

## 对照范围

- 视觉目标：已确认的 Agent 首页方案，以及 0、1、2、4 个项目的自适应状态稿。
- 实现页面：`/agent`
- 验证状态：
  - 0 个项目：新用户空态
  - 1 个项目：仅最近创作
  - 2 个项目：最近创作 + 单个其他项目
  - 4 个项目：最近创作 + 三个其他项目

## 检查结果

- Agent 输入在所有状态下保持第一视觉优先级，位置和宽度稳定。
- 最近创作在有项目时保持第二视觉优先级，不会压过输入框。
- 其他项目按数量自适应：
  - 0 个时整个项目区域隐藏。
  - 1 个时不渲染“其他项目”标题或空槽位。
  - 2 个总项目时，单个其他项目采用自然宽度横向布局。
  - 4 个以上时首页封顶展示三个其他项目。
- 新用户状态没有伪造项目卡片，使用轻量引导和主动文案。
- 文案已统一为轻松但清晰的语气，例如“回来啦”“接着来”“其他灵感现场”“先开张白纸”。
- 项目缩略图使用现有真实视觉资产，没有占位图。
- 输入建议、项目按钮和导航入口均有实际交互。
- 桌面、平板和移动端响应式规则已覆盖。
- 生产构建通过。
- 新增和修改的目标文件 ESLint 检查通过。

## 已知非阻塞项

- 全仓库 ESLint 仍会报告原有 prototype 脚本、旧配置声明和既有页面中的错误；本次修改未新增这些错误。
- Vite 仍提示既有主包体积超过 500 kB，后续可通过路由级拆包优化。

final result: passed
