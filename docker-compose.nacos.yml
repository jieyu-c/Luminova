services:
  nacos:
    image: nacos/nacos-server:v3.2.2
    container_name: luminova-nacos
    env_file:
      - .env.nacos
    environment:
      MODE: standalone
      PREFER_HOST_MODE: hostname
      NACOS_AUTH_ENABLE: "true"
    ports:
      - "8849:8080"
      - "8848:8848"
      - "9848:9848"
    volumes:
      - nacos-data:/home/nacos/data
      - nacos-logs:/home/nacos/logs
    restart: unless-stopped

volumes:
  nacos-data:
  nacos-logs:
